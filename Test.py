class Plant:
    # edible = False # через атрибут класса все работает, но Задался вопросом, возможно ли изменить значение заданное (в конструкте) при инициализации родительского класса
    def __init__(self, name): # если задавать через инит то изменить его не получется
        self.edible = False # Не могу придумать как изменить это значение при создании наследуемого класса"Fruit".
        self.name = name
        self.test = '123'

class Fruit(Plant):
    # edible = True не работает... после наследуется False из родительского класса?


    # работает но стирает инициализацию родительского класса?
    def __init__(self, заглушка):
        self.edible = True

p2 = Fruit('Заводной апельсин')
print(p2.edible)
print(p2.name)
